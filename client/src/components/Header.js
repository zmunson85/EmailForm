import React from 'react'

const Header = () => {
    return (
        <header>
            <h1>Drop Your Info Below, We will reach out shortly!</h1>
        </header>
    );
};

export default Header;
